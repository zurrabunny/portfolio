---
import Container from "/src/components/Container.astro";
---

<main class="content">
  <Container variant="static" class="container-with-bg">
    <!-- Background lines clipped to container -->
    <div class="background-clip">
    </div>

        <!-- H1 with title divider -->
    <div class="title-divider">
      <div class="line"></div>
      <h1 id="tab-title">
        <!-- Wrap each letter in a span for potential animation -->
        Let’s Get in Touch!
      </h1>
      <div class="line"></div>
    </div>

    <section class="contact-card">
      <p class="intro">
        Have my contacts!! TAKE ALL OF THEM !!<br><br>If you believe my work is a good fit for the job, please don’t hesitate to contact me! For business inquiries, feel free to email me or click the button below to reach my email even faster. For anything else<br>( personal projects or if you just wanna say hi )<br> you’re welcome to reach out on Discord. I’ll be happy to hear from you! >:]
      </p>

      <div class="contact-info">
        <h2>Contact Info</h2>
        <ul>
          <li><strong>Email:</strong> <a href="mailto:azzurraseabunny@gmail.com">azzurraseabunny@gmail.com</a></li>
          <li><strong>Discord:</strong> zurrabunny</li>
          <li><strong>Instagram:</strong> <a href="https://www.instagram.com/altzurra/" target="_blank">@altzurra ( only for close connections )</a></li>
          <li><strong>Twitter:</strong> <a href="https://x.com/azzurra_seabunn" target="_blank">@azzurra_seabunn</a></li>
        </ul>
      </div>

      <!-- Direct Gmail link -->
      <button class="btn" id="compose-email">Compose Email</button>
    </section>
  </Container>
</main>

<script>
  document.querySelector("#compose-email").addEventListener("click", () => {
    const subject = encodeURIComponent("Portfolio Inquiry");
    const body = encodeURIComponent("Hello Zurra,\n\nI would like to get in touch regarding...");
    const gmailURL = `https://mail.google.com/mail/?view=cm&fs=1&to=azzurraseabunny@gmail.com&su=${subject}&body=${body}`;
    window.open(gmailURL, "_blank");
  });
</script>

<style>
/* Clip background lines to container */
.container-with-bg {
  position: relative;
  overflow: hidden; /* ensures BackgroundLines stays inside */
}

/* Make BackgroundLines fill container */
.background-clip {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 0;
}

/* Ensure content sits above background */
.container-with-bg > :not(.background-clip) {
  position: relative;
  z-index: 1;
}

/* existing styles below... */
main.content {
  flex: 1;
  margin-left: 260px;
  padding: 2rem;
  background: none;
  color: var(--text-dark);
  position: relative;
  z-index: 1;
}
h1 { font-size: 1.8rem; margin-bottom: 1rem; color: var(--color-1); }
h2 { font-size: 1.3rem; color: var(--color-2); margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.75rem; font-weight: 600; position: relative; }
h2::before, h2::after { content: ""; flex-grow: 1; height: 2px; background-color: var(--color-5); opacity: 0.5; }
h2::before { margin-right: 0.75rem; }
h2::after { margin-left: 0.75rem; }

.contact-card {
  margin-top: 1.5rem;
  padding: 1.5rem;
  border-radius: 1rem;
  background: var(--color-6);
  border: 2px solid var(--color-5);
  max-width: 700px;
  margin-inline: auto;
  box-shadow: 0 4px 10px rgba(0,0,0,0.05);
  text-align: center;
}

.intro { margin-bottom: 1.5rem; line-height: 1.7; }
.contact-info ul { list-style: none; padding: 0; margin: 0 0 2rem 0; }
.contact-info li { margin-bottom: 0.5rem; }
.contact-info a { color: var(--color-2); text-decoration: none; transition: color 0.3s ease; }
.contact-info a:hover { color: var(--text-main); }
.btn {
  background: var(--color-2);
  color: var(--text-light);
  font-weight: bold;
  padding: 0.6rem 1.2rem;
  border: none;
  border-radius: 0.5rem;
  cursor: pointer;
  font-family: 'Fredoka', sans-serif;
  transition: background-color 0.3s ease, transform 0.2s ease;
}
.btn:hover { background: var(--color-1); transform: translateY(-2px); }

/* Mobile adjustments */
@media (max-width: 900px) {
  main.content {
    margin-left: 0;          /* Remove sidebar offset */
    padding: 1rem;           /* Reduce padding */
  }

  .contact-card {
    max-width: 100%;          /* Fill screen width */
    padding: 1rem;            /* Reduce padding */
    margin-inline: 0.5rem;    /* Small side margins */
    box-shadow: 0 2px 6px rgba(0,0,0,0.05); /* lighter shadow for small screens */
  }

  h1 {
    font-size: 1rem;
    margin-bottom: 0.75rem;
	text-align: center;
  }

  h2 {
    font-size: 1rem;
    gap: 0.5rem;
  }

  .intro {
    font-size: 0.95rem;
    line-height: 1.5;
  }

  .contact-info li {
    font-size: 0.95rem;
  }

  .btn {
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
  }

  /* Ensure background lines still fit container */
  .container-with-bg {
    overflow: hidden;
  }
  .background-clip {
    width: 100%;
    height: 100%;
  }
}

/* Title divider styling */
.title-divider {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1rem;
  gap: 1rem;
}
.title-divider .line {
  flex: 1;
  height: 2px;
  background-color: var(--color-6);
  opacity: 0;
  transform: scaleX(0);
  transform-origin: center;
}
#tab-title {
  font-size: 2rem;
  font-weight: 700;
  letter-spacing: 0.03em;
  margin: 0;
  white-space: nowrap;
  display: flex;
  justify-content: center;
}
#tab-title span {
  display: inline-block;
  opacity: 0;
}

/* Mobile adjustments */
@media (max-width: 900px) {
  #tab-title {
    font-size: 1.5rem;
    text-align: center;
    white-space: normal;
  }
  .title-divider {
    gap: 0.5rem;
  }
  .title-divider .line {
    height: 1px;
  }
}

</style>
