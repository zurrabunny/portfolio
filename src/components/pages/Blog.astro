---
import Container from "../Container.astro";
import LifeMap from "./blog_modules/LifeMap.astro";
---

<main class="content">
  <Container variant="static" class="flowchart-container">
  
    <!-- Title -->
    <div class="goal-title-line">
      <div class="goal-line"></div>
      <h1 id="goal-title">My Growth and Goals</h1>
      <div class="goal-line"></div>
    </div>
  
    <p class="flowchart-description">
      these be where my goals at, if i dont achieve them, i die (in a professional way)<br>
      Click on the nodes to highlight their path and see the SMART stuff..<br>
      note that SOME OF the smart stuff here so far are <strong>GENERATED</strong> placeholders.. I WILL change them eventually
    </p>
  
	<LifeMap/ >
		
  </Container>
</main>



<style>
main.content {
  flex: 1;
  margin-left: 260px;
  padding: 2rem;
  color: var(--text-dark);
}

/* ---------- Card ---------- */
.card {
  margin-top: 1.5rem;
  padding: 1.25rem;
  border-radius: 1rem;
  background: var(--color-6);
  border: 2px solid var(--color-5);
}

/* ---------- Headings ---------- */
h1 { font-size: 1.8rem; color: var(--color-1); margin-bottom: 1rem; }
h2 { font-size: 1.3rem; color: var(--color-2); margin-bottom: 0.5rem; margin-top: -0.5rem; }
h3 {
  font-size: 1.3rem;
  color: var(--color-2);
  margin-bottom: 0.5rem;
  position: relative;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  font-weight: 600;
}
h3::after {
  content: "";
  flex-grow: 1;
  height: 2px;
  background-color: var(--color-5);
  opacity: 0.5;
}

/* ---------- Responsive Mobile ---------- */
@media (max-width: 900px) {
  main.content { margin-left: 0; padding: 0.5rem; flex: 1; }

  .card { padding: 0.5rem; margin-top: 0.5rem; }
  .accordion-header { font-size: 1rem; border-radius: 1rem; padding: 0.6rem 0.8rem; }
  .accordion-body { padding: 0 0.5rem; }
  p, li, span, a { font-size: 0.55rem; margin: 0.5rem; }
  h2 { font-size: 0.9rem; margin: 0.3rem; }
  h3 { font-size: 0.8rem; }
}

/* Goal title line styling */
.goal-title-line {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 1rem;
  gap: 1rem;
}

.goal-line {
  flex: 1;
  height: 2px;
  background-color: var(--color-6); /* adjust to visible color */
  opacity: 1;
  transform: scaleX(1);
}

/* Title styling */
#goal-title {
  font-size: 2rem;
  font-weight: 700;
  letter-spacing: 0.03em;
  margin: 0;
  white-space: nowrap;
  display: flex;
  justify-content: center;
}

/* Mobile adjustments */
@media (max-width: 900px) {
  #goal-title {
    font-size: 1.5rem;
    text-align: center;
    white-space: normal;
  }
  .goal-title-line {
    gap: 0.5rem;
  }
  .goal-line {
    height: 1px;
  }
}

</style>
